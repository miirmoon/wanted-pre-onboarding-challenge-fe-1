const colors = {
  white: "#fff",
  black: "#000",
  grey01: "#f5f5f5",
  grey05: "#e0e0e0",
  grey10: "#9e9e9e",
  grey15: "#616161",
  "light-blue": "#d4ecff",
  blue: "#00aafe",
  yellow: "#FFC400",
  red: "#FF6F61",
  dim: "rgba(0, 0, 0, 0.6)",
};

const fontSize = {
  big: "35px",
  large: "26px",
  medium: "19px",
  normal: "17px",
  small: "14px",
  micro: "10px",
};

const fontWeight = {
  bold: 700,
  "semi-bold": 500,
  normal: 400,
  this: 300,
};

/** margin, padding */
const boxSize = {
  big: "50px",
  large: "25px",
  medium: "20px",
  normal: "16px",
  small: "13px",
  micro: "10px",
};

/** box-shadow shape */
const shadow = {
  normal: "rgba(149, 157, 165, 0.2) 0px 8px 24px",
  thin: "rgba(99, 99, 99, 0.2) 0px 2px 8px 0px",
};

/** device size */
const mediaSize = {
  minMobile: "320px",
  mobile: "420px",
};

const theme = {
  colors,
  fontSize,
  fontWeight,
  boxSize,
  shadow,
  mediaSize,
};

export default theme;
